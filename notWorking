#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <math.h>

int main()
{
    for (int i = 1000; i >= 1; i -= 5)
    {
        printf("%d\t %d\t %d\t %d\t %d\n", i, i - 1, i - 2, i - 3, i - 4);
    }

    return 0;
}
